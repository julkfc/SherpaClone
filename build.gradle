// Top-level build file for VoiceCloner
// Android本地语音克隆项目
// 基于 sherpa-onnx + ZipVoice / PocketTTS
plugins {
    id 'com.android.application' version '8.2.0' apply false
}

tasks.register('clean', Delete) {
    delete rootProject.layout.buildDirectory
}
